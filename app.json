{
  "name": "Heroku Spree Commerce",
  "description": "Image-focused, white-label Spree Commerce site integrated with Salesforce",
  "keywords": [
    "ecommerce",
    "ruby",
    "salesforce"
  ],
  "website": "https://github.com/heroku/spree-sfdc",
  "repository": "https://github.com/heroku/spree-sfdc.git",
  "success_url": "/admin",
  "scripts": {
    "postdeploy": "bin/setup"
  },
  "env": {
    "AWS_ACCESS_KEY_ID": {
      "description": "Amazon Web Services key ID with read permissions to the bucket"
    },
    "AWS_SECRET_ACCESS_KEY": {
      "description": "secret for the given Amazon Web Services key"
    },
    "AWS_REGION": {
      "description": "Amazon Web Services region; only change if the app is intentionally deployed to another region",
      "value": "us-east-1"
    },
    "AWS_S3_BUCKET": {
      "description": "Name of the S3 bucket that can be read by the Amazon Web Services key"
    },
    "DEPLOYMENT": {
      "description": "Indicates the runtime location of the app; used to regulate behaviors which are different when running at Heroku",
      "value": "production"
    },
    "LANG": {
      "description": "Default locale & character encoding to use in app runtime",
      "value": "en_US.UTF-8"
    },
    "PRICEBOOK_ID": {
      "description": "The Salesforce org's Pricebook used for synchronization of Products"
    },
    "RAILS_SERVE_STATIC_FILES": {
      "description": "Rails on Heroku should server its own static content; unset this var if a web server is in front of Spree",
      "value": "enabled"
    },
    "SECRET_KEY_BASE": {
      "description": "A unique secret used by Rails to generate and verify signed cookies (among other things)",
      "generator": "secret"
    }
  },
  "addons": [
    "heroku-postgresql",
    "herokuconnect",
    "mailgun",
    "memcachier"
  ]
}